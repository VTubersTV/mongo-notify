{
  "name": "mongo-notify",
  "version": "1.0.0",
  "description": "Real-time MongoDB change notification service with WebSocket support and diff calculation",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean"
  },
  "keywords": [
    "mongodb",
    "websocket",
    "realtime",
    "notifications",
    "change-streams"
  ],
  "author": "VTubers.TV",
  "license": "SEE LICENSE IN LICENSES.md",
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.1",
    "@vtubers.tv/types": "github:vtuberstv/types",
    "dotenv": "^16.5.0",
    "mongodb": "^6.17.0",
    "ws": "^8.18.2"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
